#Import Libraries
#Choose a LLM Model
#Chunk the docs
#Embed the docs
#Store the embeddings in a Vector DB
#Logic
  #User Query
  #Convert USer query into Embeddings
  #Do a semantic similarity search with the vectors in the VEctor DB
  #Get the similar data
  #Pass it as context to the LLM (RAG)
  #Get the response
  #Pass the response to the User



